# 이미지에 들어가면 안 되는 것들 (.gitignore와 같은 기준)
.git
.gitignore
.dockerignore

# Python
__pycache__/
*.py[cod]
.pytest_cache/

# 가상환경
venv/
.venv/
env/

# 로컬 DB — 컨테이너에 로컬 SQLite 파일을 굽지 않는다
*.db
*.sqlite3

# 환경변수 (실제 키는 플랫폼 환경변수로 주입한다)
.env
.env.local

# 에디터/OS
.vscode/
.idea/
.DS_Store
Thumbs.db
