# Leave this unset during local development to use Vite's /api proxy.
# For a separately deployed API, set its public URL including /api.
# VITE_API_BASE_URL=https://api.example.com/api

# Optional: local proxy target used by `npm run dev`.
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000
